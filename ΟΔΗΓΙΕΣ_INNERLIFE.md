# 📖 Οδηγίες Διαχείρισης — Innerlife Site

---

## 🚀 Πώς να κάνεις Deploy (Ανέβασμα στο Internet)

Κάθε φορά που κάνεις αλλαγή στο `index.html`:

1. Άνοιξε το **Terminal** (Finder → Εφαρμογές → Βοηθητικά → Terminal)
2. Γράψε:
```bash
cd ~/Desktop/BACKUP
netlify deploy --prod --dir=.
```
3. Πάτα Enter — σε 30 δευτερόλεπτα το site ενημερώνεται!

---

## 📝 Πώς να Επεξεργαστείς Κείμενα στις Πύλες

Άνοιξε το `index.html` με **TextEdit** (κλικ δεξί → Άνοιγμα με → TextEdit).

### Βρες την ενότητα που θέλεις

Χρησιμοποίησε **⌘F** (Find) και ψάξε:

| Πύλη | Ψάξε για |
|------|----------|
| Γνώση & Φιλοσοφία | `WISDOM_CONTENT` |
| Γαλήνη & Σχέσεις | `MYSTERY_CONTENT` ή `RELATIONS_CONTENT` |
| Υγεία & Καθημερινότητα | `HEALTH_CONTENT` ή `DAILY_CONTENT` |
| Ελπίδα & Επιστήμη | `FAITH_CONTENT` ή `SCIENCE_CONTENT` |
| Φιλοσοφία | `PHILOSOPHY_CONTENT` |
| Αυτογνωσία (Κουίζ) | `QUIZ_QUESTIONS` |
| Μονοπάτι 50 Ημερών | `PATH_30` |

### Μορφή κειμένου

Κάθε κείμενο μοιάζει έτσι:
```javascript
{ type:'text', title:'Ο Τίτλος', body:'Το κείμενο εδώ...' },
```

### ➕ Για να ΠΡΟΣΘΕΣΕΙΣ κείμενο
Βρες το τέλος της λίστας (πριν από `];`) και πρόσθεσε:
```javascript
{ type:'text', title:'Ο νέος τίτλος', body:'Το νέο κείμενο...' },
```
> ⚠️ **Πρόσεχε:** Μην χρησιμοποιείς μονά εισαγωγικά `'` μέσα στο κείμενο.
> Χρησιμοποίησε αντ' αυτού `'` (curved apostrophe) ή διπλά `"` εισαγωγικά.

### ✏️ Για να ΑΛΛΑΞΕΙΣ κείμενο
Βρες τον τίτλο με ⌘F, επεξεργάσου το `body:` ή `title:`.

### ❌ Για να ΔΙΑΓΡΑΨΕΙΣ κείμενο
Σβήσε ολόκληρη τη γραμμή (από `{` μέχρι `},`).

---

## ❓ Πώς να Προσθέσεις Ερώτηση στην Αυτογνωσία

Βρες `QUIZ_QUESTIONS` και πριν το `];` πρόσθεσε:
```javascript
{ text:'Η ερώτησή σου εδώ',
  dest:'mystery-relations',
  label:'Γαλήνη & Σχέσεις',
  color:'#ff8099',
  emoji:'♡' },
```

### Τιμές για `dest` (Πύλη προορισμού):
| dest | Πύλη |
|------|------|
| `mystery-relations` | Γαλήνη & Σχέσεις |
| `wisdom-philosophy` | Γνώση & Φιλοσοφία |
| `photos-daily` | Υγεία & Καθημερινότητα |
| `wisdom2-science` | Ελπίδα & Επιστήμη |
| `meditation` | Διαλογισμός & Νους |
| `church` | Πύλη Προσευχής |
| `music-direct` | Μουσική |

---

## 🌱 Πώς να Προσθέσεις Πρόκληση στο Μονοπάτι

Βρες `PATH_30` και πριν το `];` πρόσθεσε:
```javascript
{ day:51, title:'Τίτλος Ημέρας',
  text:'Η πρόκληση/εισήγηση για αυτή την ημέρα...' },
```
> Άλλαξε τον αριθμό `day` αντίστοιχα (συνέχεια από τον τελευταίο).

---

## ⏱️ Αλλαγή Χρόνων Animation

### Animation Προσευχής (λέξεις που ανεβαίνουν)

Βρες `_animatePrayerRising` και άλλαξε:
```javascript
}, i * 700);  /* ← χρόνος μεταξύ λέξεων (ms) - μεγαλύτερο = πιο αργά */
```
```javascript
el.style.animationDuration = (3.2 + Math.random() * 0.6) + 's'; /* ← διάρκεια κάθε λέξης */
```

### Animation Εισαγωγής (fade in)
Βρες `animation:fadein` — αύξησε τον αριθμό (`0.55s`) για πιο αργό.

---

## 🎨 Αλλαγή Χρωμάτων

### Χρώμα Πύλης (arch-label)
Βρες π.χ. `Γνώση & Φιλοσοφία` και άλλαξε `color:#ffe060`.

### Χρώμα Κυκλών Αυτογνωσίας/Μονοπατιού
Βρες `.gold-circle-btn` και άλλαξε τα χρώματα στο `background:radial-gradient(...)`.

### Κύριο χρώμα φόντου
Βρες `#060c1a` (το σκούρο μπλε-μαύρο) και άλλαξέ το.

---

## 🔍 Αλλαγή SEO (Λέξεις-Κλειδιά)

Βρες `<meta name="keywords"` και επεξεργάσου τη λίστα λέξεων.
Βρες `<meta name="description"` για να αλλάξεις την περιγραφή.

---

## 🌐 Γλώσσες

Το site υποστηρίζει: **Ελληνικά, English, Русский, Română** (native) + Google Translate για τα υπόλοιπα.

Για να προσθέσεις νέα native γλώσσα, βρες `EP_T` στο JavaScript και πρόσθεσε:
```javascript
bg: { /* Βουλγαρικά */
  title: 'Вътрешен живот',
  sub: 'Пространство за размисъл',
  enter: 'ВХОД',
}
```

---

## 📁 Δομή Αρχείων (BACKUP folder)

```
BACKUP/
├── index.html          ← Το κύριο αρχείο (εδώ κάνεις ΟΛΕΣ τις αλλαγές)
├── manifest.json       ← PWA settings
├── sw.js               ← Service Worker
├── icon-192.png        ← App icon
├── icon-512.png        ← App icon (μεγάλο)
├── church.jpg          ← Φόντο εκκλησίας
└── *.m4a, *.mp3        ← Αρχεία μουσικής
```

---

## ⚠️ Χρήσιμες Προφυλάξεις

1. **Πάντα** κράτα backup πριν αλλάξεις κάτι: αντέγραψε το `index.html` ως `index_backup.html`
2. Μην αφαιρείς **ποτέ** τα `const`, `let`, `function`, `];` — είναι κρίσιμα για τον κώδικα
3. Αν κάτι σπάσει: επαναφορά από backup και ξανά deploy
4. Για μεγάλες αλλαγές: ζήτα βοήθεια από την Claude!

---

## 📞 Γρήγορη Βοήθεια

Αν κάτι δεν δουλεύει, δες την Console του browser:
- **Mac:** ⌘ + Option + J (Chrome) ή ⌘ + Option + I (Safari)
- Αν βλέπεις κόκκινο σφάλμα, αντέγραψέ το και στείλε το στην Claude

---

*Τελευταία ενημέρωση: Ιούλιος 2026*
